Extended Example
================

This example shows:

- setting additional defaults at runtime
- setting default functions
- defining additional custom config types
- defining a post-processing pipeline
- adding custom config values at runtime


Prepare the two needed ``configuration definition files`` to run this example:

- "example_2_intermediate.yaml" (See the :doc:`doc_example_2_intermediate` section)
- "example_4_extended.yaml" (See the :doc:`doc_example_4_extended` section)


Run the following code example:

.. literalinclude:: ../../example_usage/example_4_extended.py
   :language: python
   :linenos:
   :lines: 4-


Further Reading
---------------

See the :doc:`../doc_api` section for the full API reference.
