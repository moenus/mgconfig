# mgconfig configuration definitions 
# for basic usage example

- section: application
  prefix: app
  configs:
  - name: test
    type: str
    readonly: True
    default: teststring
    description: this configuration is used to demonstrate the basic usage of mgconfig
